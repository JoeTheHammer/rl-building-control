experiments:
  - name: "ppo-base"
    engine: "sinergym"
    environment_config: 
      /config/environments/sinergym-data-center-evaluation.yaml
    controller: "ppo"
    controller_config: /config/controllers/ppo-controller.yaml
    episodes: 10
    seed: 42
    reporting:
      denormalize_state: true
